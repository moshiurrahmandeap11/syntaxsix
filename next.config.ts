import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
    images: {
    domains: ["img.freepik.com", "images.unsplash.com", "plus.unsplash.com", "lh3.googleusercontent.com"], 
  },
};

export default nextConfig;
